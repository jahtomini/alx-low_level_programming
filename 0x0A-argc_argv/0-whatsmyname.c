#include <stdio.h>

/**
 * main - Print the program's file name.
 *
 * @argc: Number of arguments.
 * @argv: Array of arguments as strings.
 *
 * Return: Always 0.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
