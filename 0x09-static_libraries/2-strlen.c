#include "main.h"
#include <string.h>

/**
  * _strlen - _strlen
  * Description - return the length of a string
  * @s: the string being pointed at
  *
  * Return: empty
  *
  */


int _strlen(char *s)
{
	return (strlen(s));
}

