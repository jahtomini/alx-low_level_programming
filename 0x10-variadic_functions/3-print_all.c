#include <stdarg.h>
#include <stdio.h>
#include <stdio.h>

/**
 * print_all - A function that prints anything
 *
 * @format: A string that is actually a list of types of arguments passed to
 * the function
 *
 * Return: void
 *
 */

void print_all(const char * const format, ...)
{
	unsigned int i = 0;
	va_list ap;

	va_start(ap, n);

	// get strlen(format)
	// use a while loop to iterate over format while i < strlen
	// if char in format == c || i || f || s
	// 

	printf("\n");

	va_end(ap);
}
